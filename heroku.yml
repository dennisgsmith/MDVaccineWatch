setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE_URL
  config:
    WEB_CONCURRENCY: 2
    PYTHONUNBUFFERED: "stout"
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn app:server