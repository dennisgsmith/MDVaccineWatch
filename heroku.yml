setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: process-scheduler:pet-project
  config:
    PYTHONUNBUFFERED: "stout"
    WEB_CONCURRENCY: 2
    PORT: 8050
build:
  docker:
    web: app/Dockerfile
    scheduler: scheduler/Dockerfile
run:
  web: gunicorn app:server
  scheduler: python main.py