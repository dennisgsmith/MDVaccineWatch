setup:
  addons:
    - plan: scheduler
  config:
    AWS_S3_BUCKET: mdvaccinewatch-objstore
    AWS_DEFAULT_REGION: us-east-1
build:
  docker:
    web: app/Dockerfile
    scheduler: backend/Dockerfile
run:
  web: ./gunicorn_starter.sh